# -----------------------------------------------------------------------------
# 网关公网/本地根地址（与事件回调、OID 文档中的 Gateway URL 一致）
# -----------------------------------------------------------------------------
GATEWAY_PUBLIC_BASE_URL=http://127.0.0.1:8000

# -----------------------------------------------------------------------------
# 飞书云盘：目标目录与开发开关
# 目录 folder_token 见云空间 URL（fld...）；接真飞书时必填。DEV_SKIP 时列表可为占位
# -----------------------------------------------------------------------------
ARTIFACTS_DRIVE_FOLDER_TOKEN=https://jcneyh7qlo8i.feishu.cn/drive/folder/MWftf8OcLl0uFrdqAaVcpSUMnpe
DEV_SKIP_LARK=true

# -----------------------------------------------------------------------------
# 外部 Agents 服务
# -----------------------------------------------------------------------------
AGENTS_BASE_URL=http://127.0.0.1:18080
AGENTS_M2M_TOKEN=dev-m2m-secret

# -----------------------------------------------------------------------------
# 飞书开放平台：应用凭证（从控制台复制；勿提交到 Git 真实 .env）
# -----------------------------------------------------------------------------
LARK_APP_ID=cli_a966e2536d799bcd
LARK_APP_SECRET=6KicdCBjUynCM9jYIf9IQe2I3GaADBeG

# -----------------------------------------------------------------------------
# 飞书事件：订阅/加解密（在开放平台启用事件时填写；未用可留空开发）
# -----------------------------------------------------------------------------
LARK_EVENT_ENCRYPT_KEY=
LARK_VERIFICATION_TOKEN=

# -----------------------------------------------------------------------------
# 前端 H5 与 OAuth 重定向；须与「网页应用/安全」里域名与回调一致
# -----------------------------------------------------------------------------
PUBLIC_WEB_BASE_URL=http://127.0.0.1:5173
OAUTH_REDIRECT_URI=http://127.0.0.1:8000/api/v1/auth/callback

# -----------------------------------------------------------------------------
# 会话：Cookie 签名（非业务数据库）
# -----------------------------------------------------------------------------
SESSION_SECRET=change-me-in-production

# -----------------------------------------------------------------------------
# 历史保留：Gateway 已改走 OpenAPI，不再调用 lark-cli；可删或留空
# -----------------------------------------------------------------------------
LARK_CLI_PATH=lark-cli

# 业务与产物在飞书云盘；Gateway 不配置本地业务库
