# lark_cli_driver

Gateway 对 `lark-cli` 子进程的薄封装，默认附加 `--format json` 与 `--as user`（在参数未显式带 `--as` 时）。

安装（开发，从 monorepo 根或 gateway venv 用 editable）：

```bash
pip install -e packages/lark_cli_driver
```
